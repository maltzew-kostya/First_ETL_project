# Вспомним метод Counter
from collections import Counter

lst = list("мама мыла раму")
res_dct = Counter(lst)
print(res_dct.most_common(3))

# вносим изменения 1
print(0.1 + 0.2 + 0.1)

# внесу новые изменения
print(0.3)
